<!DOCTYPE html>
<html>
  <head>
    <%= display_meta_tags(default_meta_tags) %>

    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+jjXkk+Q2h455rYXK/7HAuoJl+0I4" crossorigin="anonymous"></script>
    <script src="https://unpkg.com/feather-icons"></script>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg bg-light">
      <div class="container">
        <a class="navbar-brand" href="/">Fuji X Recipes</a>
        <div>
          <div class="d-inline-block d-md-none">
            <a href="<%= new_recipe_path %>" class="btn btn-outline-success"><i data-feather="plus"></i></a>
            <a href="<%= saved_user_recipes_path(current_user) %>" class="btn btn-outline-secondary"><i data-feather="bookmark"></i></a>
          </div>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
        </div>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <div class="me-auto mb-2 mb-lg-0"></div>
          <a class="btn btn-outline-success d-none d-md-inline-block me-3" href="<%= new_recipe_path %>">+ Add recipe</a>

          <% if user_signed_in? %>
            <a class="btn btn-link" href="<%= user_path(current_user) %>">Your profile</a>
            <a class="btn btn-link d-none d-md-inline-block" href="<%= saved_user_recipes_path(current_user) %>">Saved recipes</a>
            <%= button_to 'Sign-out', destroy_user_session_path, class: 'btn btn-link', method: :delete, data: { turbo: false } %>
          <% else %>
            <%= button_to 'Sign-in', new_user_session_path, class: 'btn btn-link', data: { turbo: false } %>
          <% end %>
        </div>
      </div>
    </nav>

    <%= yield :hero %>

    <div class="container">
      <% if notice %>
        <div class="toast-container position-fixed bottom-0 end-0 p-3">
          <div id="liveToast" class="toast fade show text-bg-info" style="--bs-bg-opacity: .8;" role="alert" aria-live="assertive" aria-atomic="true" data-bs-autohide="true">
            <div class="d-flex">
              <div class="toast-body">
                <%= notice %>
              </div>

              <button type="button" class="btn-close me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
            </div>
          </div>
        </div>
      <% end %>

      <%= yield %>
    </div>

    <script>
      feather.replace()
    </script>
  </body>
</html>
