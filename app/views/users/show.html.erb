<% set_meta_tags title: @user.username, description: "Recipes posted by #{@user.username}", canonical: user_url(@user) %>

<%= render layout: 'users/profile_layout' do %>
  <%= render 'recipes/recipes', recipes: @recipes, pagy: @pagy, saves: @saves %>
<% end %>
